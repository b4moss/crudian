# テスト仕様 v0.6.0

対象マイルストーン: v0.6.0（libSQL adapter）  
関連 Issue: [#42](https://github.com/b4moss/crudian/issues/42)  
仕様: [`../main.md`](../main.md)  
設計: [`../plans/libsql-adapter.md`](../plans/libsql-adapter.md)  
ロードマップ: [`../plans/roadmap.md`](../plans/roadmap.md)  
前提: [`./v0.1.0.md`](./v0.1.0.md) / [`./v0.2.0.md`](./v0.2.0.md) / [`./v0.3.0.md`](./v0.3.0.md) / [`./v0.5.0.md`](./v0.5.0.md)  
書き方: charter `tdd.md`（氷山パターン）

本ファイルは **#42（`@b4moss/crudian/libsql`）** の受け入れを扱う。  
サブパスは商業サービス名 Turso ではなく **libSQL**（SDK: `@libsql/client`）。

## 共通前提

| 項目 | 値 |
|------|-----|
| 対象 | `@b4moss/crudian/libsql` |
| 契約 | bun-sqlite / prisma と同等の語彙・振る舞い（現行契約: CRUD / search / list / count / total / upsert / duplicate / bulk* / transaction） |
| 共有 | 型・`where` ビルダー・`CrudianError` は `@b4moss/crudian`。実装は `sqlite/async-crud` を再利用 |
| ランタイム / テスト | **Node.js 24+** + `node:test`（`node --test` / `tsx --test`）。アダプタ固有（共有スイートなし） |
| DB | `createClient({ url: ":memory:" })`。呼び出し側がクライアントを注入。クラウド資格情報は使わない |
| 入口 | `createCrud(client)`。パスや接続を内部生成しない |
| upsert / bulkUpsert conflict | 主キー `id` |
| 単発書き込み戻り値 | 対象行（`read` / `update` / `duplicate` の0件は `null`）。すべて `Promise` |
| bulk 戻り値 | 件数のみ（`Promise<number>`） |
| `SearchResult` | `{ items, nextCursor, hasMore, total }`（v0.5.0 契約） |
| `count` | `CountQuery`（`{ where? }`）→ `Promise<number>` |
| cursor | 当面 `id` 昇順。`nextCursor` は生の `id` |
| 識別子 | 文字列必須。形式検証なし（下位例外に任せる） |
| 独自エラー | 最小限。他は下位（`@libsql/client`）例外を伝播 |
| 対象外 | リモート Turso Cloud E2E、`@tursodatabase/serverless`、TypeORM、PHP / Go、全文検索、offset pagination |

フィクスチャ表（例）は整数主キー `id` を持つ単表。スキーマはテスト内 DDL で用意してよい。  
**同等性の判定:** 同じ操作列に対し、prisma（async）および bun-sqlite 仕様（v0.1 / v0.2 / v0.5）と同じ観測結果になること。

---

## libsql アダプタ

パッケージ: `@b4moss/crudian/libsql`  
入口: `createCrud(client)`（`@libsql/client` の Client 相当を注入。生クライアントを `crud.db` で公開）

### libsql.createCrud

- 注入した client と同じ参照を Facade から読める
- `create` / `read` / `update` / `delete` / `upsert` / `duplicate` / `bulk*` / `search` / `list` / `count` / `transaction` を持つ
- パスや接続を内部生成しない

#### テスト：正常系

- 注入クライアントと `crud.db` が同一参照
- 上記メソッドがすべて関数として存在する
- `:memory:` クライアントを渡して初期化エラーなく使える

#### テスト: 異常系

- `client` が `null` / `undefined` のとき拒否する
- `client` が想定外型（`execute` 欠落など）のとき拒否する

---

### libsql.transaction

- コールバックを interactive トランザクション内で実行する（`transaction("write")`）
- 成功時コミット、例外時ロールバック
- 各 CRUD は自動では TX を張らない

#### テスト：正常系

- コールバック内の複数 `create` がコミットされ、終了後にすべて読める
- コールバックの戻り値が伝わる

#### テスト: 異常系

- コールバック throw でロールバックされ、部分書き込みが残らない
- 非関数コールバックは拒否する

---

### libsql.coreCrud

`create` / `read` / `update` / `delete`（v0.1.0 相当・async）

#### テスト：正常系

- `create` が対象行を返し、`id` が採番される
- `read` が1件を返す。カラム制限ができる
- `update` が更新後行を返す。未指定カラムは維持
- `delete` が影響件数を返す

#### テスト: 異常系

- `read` 未ヒットは `null`
- `update` 0件は `null`
- `delete` 0件は `0`
- テーブル名が文字列でないとき拒否
- 制約違反は下位例外

---

### libsql.searchList

`search`（正式）/ `list`（別名）、条件ビルダー、cursor（v0.1.0 + v0.5.0 の `total`）

#### テスト：正常系

- `items` が `id` 昇順
- `limit` 未満で `hasMore=false`、`nextCursor=null`
- ページングで続きが重複なく取れる
- `list` と `search` が同結果（`total` 込み）
- `eq` / `and` / `or` / 比較 / `in` / `like` / `isNull` / `isNotNull` が絞り込める
- 返却に `total` があり、where 全件数と一致する（limit / cursor 非依存）

#### テスト: 異常系

- `limit` 不正は拒否
- `in` 空配列は拒否
- カラム名が文字列でないとき拒否

---

### libsql.count

`count`（v0.5.0 相当・async）

#### テスト：正常系

- 空表で `count(table)` が `0`
- 複数行挿入後、行数と一致
- `where` ありで一致件数のみ
- 条件0件は `0`（throw しない）
- Facade に `count` が関数として存在する

#### テスト: 異常系

- テーブル名が文字列でないとき拒否
- 存在しないテーブル参照は下位例外
- `in` 空配列など where 由来の拒否が `search` と同じ

---

### libsql.extendedWrites

`upsert` / `duplicate` / `bulk*`（v0.2.0 相当・async）

#### テスト：正常系

- `upsert`: 未存在 id は挿入、既存 id は更新。未指定カラムは更新時維持
- `duplicate`: 新 id でコピー。overrides 反映。0件は `null`
- `bulkCreate` / `bulkUpdate` / `bulkDelete` / `bulkUpsert` が件数のみ返す
- 空配列の bulk は `0`（no-op）

#### テスト: 異常系

- `upsert` / `bulkUpsert` で `id` 欠落は拒否
- `duplicate` / `bulkUpdate` / `bulkDelete` で where 欠落は拒否
- 制約違反は下位例外

---

## パッケージ境界

### libsqlExports

- Node から `@b4moss/crudian/libsql` を import できる
- 共有契約 `@b4moss/crudian` を Node から import できる
- Bun 専用の `bun-sqlite` を Node から読んだ場合の誤 import 明示は既存どおり維持

#### テスト：正常系

- Node 24 で `@b4moss/crudian/libsql` を import できる
- 共有契約を import できる

#### テスト: 異常系

- Node から `@b4moss/crudian/bun-sqlite` を読むと誤 import 明示エラー（既存・回帰）

---

## CI

### libsqlCi

- libsql のテストは Node 24 + `node:test` で CI から実行できること

#### テスト：正常系

- `bun run test:libsql`（または同等スクリプト）が Node 上で通る
- 既存の `test:drizzle` / `test:prisma` / `test:bun-sqlite` を壊さない

#### テスト: 異常系

- （該当なし。失敗時は CI 赤）

---

## 回帰（スモーク）

### regressionWithLibsql

- 既存3アダプタのテストが引き続き通る
- libsql 追加で共有契約の破壊的変更を入れない（必要なのは subpath / peer / docs）

#### テスト：正常系

- 既存アダプタのスモーク（create → read、search の cursor、count）が通る

#### テスト: 異常系

- （該当なし）

----

以上
